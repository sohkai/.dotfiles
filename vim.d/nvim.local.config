" vim: set ft=vim:
set runtimepath^=~/.vim runtimepath+=~/.vim/after
let &packpath = &runtimepath

let s:config_loc = expand('$HOME/.vimrc')
if filereadable(s:config_loc)
  execute 'source '.fnameescape(s:config_loc)
endif
