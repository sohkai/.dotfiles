# Source .zshrc
[[ -r ~/.dotfiles/.zshrc ]] && . ~/.dotfiles/.zshrc

# Load local profile
[[ -r ~/.profile_env ]] && . ~/.profile_env

# Load language version defaults
[[ -r ~/.language_ver ]] &&  ask "Load languages?" N && . ~/.language_ver
