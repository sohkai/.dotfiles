[[ -r ~/.dotfiles/.bashrc ]] && . ~/.dotfiles/.bashrc

# Load local profile
[[ -r ~/.profile_env ]] && . ~/.profile_env

# Load local language version defaults
[[ -r ~/.language_ver ]] &&  ask "Load languages?" N && . ~/.language_ver
