# Load synced .bashrc
source_file ~/.dotfiles/bash/bashrc

# Load local profile
source_file ~/.profilerc

# Load local language configuration, if confirmed
[[ -r ~/.languagerc ]] &&  ask "Load languages?" N && . ~/.languagerc
