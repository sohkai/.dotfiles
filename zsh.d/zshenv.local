# zshenv is *always* loaded, so it's as lightweight as possible and doesn't source the utilities

# Load initializing environment
[[ -r $HOME/.init_env ]] && source $HOME/.init_env

# Load synced .zshenv
[[ -r $DOTFILES/zsh.d/zshenv ]] && source $DOTFILES/zsh.d/zshenv
